/** Max files per estimate request submission (public + internal). */
export const MAX_REQUEST_ATTACHMENT_FILES = 10;

/** Max total raw upload bytes per request submission. */
export const MAX_REQUEST_ATTACHMENT_TOTAL_BYTES = 10 * 1024 * 1024;

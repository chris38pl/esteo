/** Form-focused dashboard pages (e.g. workspace settings tabs). */
export const dashboardPageFormClass = "mx-auto w-full max-w-[560px] py-8";

/** Narrow dashboard content column (referrals, focused panels). */
export const dashboardPageNarrowClass =
  "mx-auto flex min-w-0 w-full max-w-5xl flex-1 flex-col pb-8";

/** Wide dashboard content column (billing, workspace usage). */
export const dashboardPageWideClass =
  "mx-auto flex min-w-0 w-full max-w-[1400px] flex-1 flex-col pb-8";

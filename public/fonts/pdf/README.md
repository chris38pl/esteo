Bundled Inter WOFF2 subsets for estimate PDF rendering (latin, weights 400/600/700).

Copied from `@fontsource/inter` — do not edit manually unless updating the font version.

Also copied to Trigger.dev workers via `additionalFiles` in `trigger.config.ts`.
